program two-exponents(in : integer);
  function exponent(n:integer) : integer
    begin  if n = 1 then
	     return 1
	   else
	     return 2 * exponent(n - 1)
    end;
begin
  return exponent (in)
end.
